# 學生成績登錄系統 - 逐步實作指引

## 前言

本指引將帶領你一步步完成學生成績登錄系統的開發。建議按照順序實作，每完成一個階段就測試一次，確保功能正常運作。

## 開發環境準備

### 步驟 1：建立 Google Sheets 專案

1. 開啟 Google Sheets，建立新試算表
2. 將試算表命名為「學生成績管理系統」
3. 點選「擴充功能」→「Apps Script」
4. 將專案命名為「成績登錄系統」

### 步驟 2：啟用 V8 Runtime（選用）

1. 在 Apps Script 編輯器中，點選左側「專案設定」
2. 勾選「啟用 Chrome V8 runtime」
3. 儲存設定

> 💡 提示：本專案使用完全相容的語法，不啟用 V8 也能正常運作。

---

## 階段 1：初始化試算表（15 分鐘）

### 目標
建立試算表的標題列並套用基本格式。

### 實作步驟

#### 1.1 撰寫 initializeSheet() 函式

```javascript
function initializeSheet() {
  var sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
  
  // 設定標題列
  var headers = ['學號', '姓名', '國文', '英文', '數學', '平均', '等級'];
  sheet.getRange(1, 1, 1, headers.length).setValues([headers]);
  
  // 套用標題列格式
  var headerRange = sheet.getRange(1, 1, 1, headers.length);
  headerRange.setFontWeight('bold');
  headerRange.setBackground('#cccccc');
  headerRange.setHorizontalAlignment('center');
  
  Logger.log('試算表初始化完成');
}
```

